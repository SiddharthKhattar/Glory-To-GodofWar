// Program to make pyramid
#include <stdio.h>
#include <stdlib.h>

int main() {
    int num, x, y;
    printf("Enter number of columns for pyramid: ");
    scanf("%d", &num);
    i = -num;
    for (y = 0; y <= num; y++) {
        for( i = -num; i <= num; i++); {

